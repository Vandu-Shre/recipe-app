meta {
  name: Delete a rating
  type: http
  seq: 4
}

delete {
  url: {{hostname}}/api/ratings/{{ratingId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
