meta {
  name: Delete a rating
  type: http
  seq: 4
}

delete {
  url: {{hostname}}/api/ratings/{{ratingId}}
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
