meta {
  name: Get all ratings in a recipe
  type: http
  seq: 1
}

get {
  url: {{hostname}}/api/ratings/{{recipeId}}
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
