meta {
  name: Get single recipe by ID
  type: http
  seq: 2
}

get {
  url: {{hostname}}/api/recipes/{{recipeId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}
