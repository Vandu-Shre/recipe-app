meta {
  name: Get all recipes
  type: http
  seq: 1
}

get {
  url: {{hostname}}/api/recipes
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
