meta {
  name: Delete a recipe
  type: http
  seq: 4
}

delete {
  url: {{hostname}}/api/recipes/{{recipeId}}
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
