meta {
  name: Update password
  type: http
  seq: 5
}

put {
  url: {{hostname}}/api/auth/update-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  { 
    "currentPassword": "test1234", 
    "newPassword": "newSecurePassword123" 
  } 
}

settings {
  encodeUrl: true
}
