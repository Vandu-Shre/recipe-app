meta {
  name: Get user profile
  type: http
  seq: 3
}

get {
  url: {{hostname}}/api/auth/profile
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
}
